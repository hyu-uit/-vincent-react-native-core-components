import { COLORS } from './colors';

export const PHONE_INPUT_COLORS = {
  background: COLORS.white,
  text: COLORS.black,
  placeholder: COLORS.gray400,
  border: COLORS.gray300,
  borderFocused: COLORS.primary,
  borderError: COLORS.danger,
};
