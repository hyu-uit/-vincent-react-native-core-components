import { COLORS } from './colors';

export const SKELETON_IMAGE_COLORS = {
  skeleton: COLORS.gray100,
  shimmer: COLORS.white,
  errorBackground: COLORS.gray100,
  errorText: COLORS.gray400,
  errorIcon: COLORS.gray400,
};
